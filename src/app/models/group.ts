export class Group {
    id?: number;
    number?: string;
    name: string;
    subst?: number;
    presentation?: Array<any>;
}

export class Groupes {
    grp_name: string;
    code_prdt?: Array<number>;
    code_pdct?: Array<number>;
}
